import { router, Stack, useLocalSearchParams } from "expo-router";
import { Pressable, ScrollView, Text, View } from "react-native";
import { useMemo, useState } from "react";
import { usePortfolio, calculatePositions, normalizeTicker } from "@/lib/portfolio";
import { suggestFunds, findFund } from "@/lib/fii-catalog";
import { ScreenContainer } from "@/components/screen-container";
import { Field, Help } from "@/components/form-fields";
import { Button, Card } from "@/components/portfolio-ui";
import { brDateToIso, currency, currencyInput, isoDateToBr, number, parseCurrencyInput } from "@/lib/format";
const dateInput = (value: string) => { const digits = value.replace(/\D/g, "").slice(0, 8); return digits.length <= 2 ? digits : digits.length <= 4 ? `${digits.slice(0, 2)}/${digits.slice(2)}` : `${digits.slice(0, 2)}/${digits.slice(2, 4)}/${digits.slice(4)}`; };
const today = isoDateToBr(new Date().toISOString().slice(0, 10));
export default function TransactionForm() { const params = useLocalSearchParams<{ ticker?: string; id?: string }>(); const { operations, quotes, addOperation, updateOperation } = usePortfolio(); const old = operations.find((item) => item.id === params.id); const [kind, setKind] = useState<"buy" | "sell">(old?.kind ?? "buy"); const [ticker, setTicker] = useState(old?.ticker ?? params.ticker ?? ""); const [date, setDate] = useState(old ? isoDateToBr(old.date) : today); const [quantity, setQuantity] = useState(old ? String(old.quantity) : ""); const [price, setPrice] = useState(old ? currencyInput(String(Math.round(old.price * 100))) : ""); const [fees, setFees] = useState(old ? currencyInput(String(Math.round(old.fees * 100))) : ""); const [note, setNote] = useState(old?.note ?? ""); const [error, setError] = useState(""); const suggestions = useMemo(() => suggestFunds(ticker), [ticker]); const selected = findFund(ticker); const normalized = normalizeTicker(ticker); const quote = quotes[normalized]; const preview = useMemo(() => { const q = Number(quantity.replace(",", ".")); const p = parseCurrencyInput(price); const f = parseCurrencyInput(fees); const operationDate = brDateToIso(date); if (!normalized || q <= 0 || p <= 0 || !/^\d{4}-\d{2}-\d{2}$/.test(operationDate)) return null; const base = old ? operations.filter((item) => item.id !== old.id) : operations; const [position] = calculatePositions([...base, { id: "preview", ticker: normalized, kind, date: operationDate, quantity: q, price: p, fees: f, createdAt: "9999" }], quotes); return position ?? null; }, [date, fees, kind, normalized, old, operations, price, quantity, quotes]); const choose = (choice: string) => { setTicker(choice); const current = quotes[choice]; if (current && !old) setPrice(currencyInput(String(Math.round(current.price * 100)))); }; const save = () => { try { setError(""); const input = { ticker, kind, date: brDateToIso(date), quantity: Number(quantity.replace(",", ".")), price: parseCurrencyInput(price), fees: parseCurrencyInput(fees), note: note.trim() || undefined }; if (old) updateOperation(old.id, input); else addOperation(input); router.back(); } catch (reason) { setError(reason instanceof Error ? reason.message : "Não foi possível salvar."); } }; return <ScreenContainer className="px-5" edges={["top", "bottom", "left", "right"]}><Stack.Screen options={{ headerShown: false }} /><ScrollView showsVerticalScrollIndicator={false} contentContainerClassName="pb-8"><Text className="mt-2 text-sm font-medium text-muted">Lançamento inteligente</Text><Text className="mt-1 text-3xl font-bold text-foreground">{old ? "Editar movimentação" : "Nova movimentação"}</Text><Text className="mt-3 text-sm leading-5 text-muted">O app sugere o FII e recalcula o PM conforme você digita.</Text><View className="mt-6 flex-row gap-3 rounded-2xl bg-surface p-2"><View className="flex-1"><Button compact title="Compra" variant={kind === "buy" ? "primary" : "secondary"} onPress={() => setKind("buy")} /></View><View className="flex-1"><Button compact title="Venda" variant={kind === "sell" ? "primary" : "secondary"} onPress={() => setKind("sell")} /></View></View><View className="mt-6"><Field label="Ticker do FII" value={ticker} onChangeText={setTicker} placeholder="Digite HGLG11 ou logística" autoCapitalize="characters" /><View className="-mt-3 mb-3">{suggestions.map((fund) => <Pressable key={fund.ticker} onPress={() => choose(fund.ticker)} style={({ pressed }) => ({ backgroundColor: "#DFF4FA", borderBottomColor: "#A7DDE8", borderBottomWidth: 1, opacity: pressed ? 0.65 : 1, paddingHorizontal: 12, paddingVertical: 10 })}><Text className="font-bold text-foreground">{fund.ticker}</Text><Text className="text-xs text-muted">{fund.name} · {fund.segment}</Text></Pressable>)}</View>{selected ? <Text className="mb-4 text-xs font-medium text-primary">{selected.name} · {selected.segment}{quote ? ` · última cotação ${currency(quote.price)}` : ""}</Text> : null}<Field label="Data da operação" value={date} onChangeText={(value) => setDate(dateInput(value))} placeholder="DD/MM/AAAA" /><Help>Use o formato DD/MM/AAAA.</Help><Field label="Quantidade de cotas" value={quantity} onChangeText={setQuantity} placeholder="0" keyboardType="numeric" /><Field label="Preço por cota" value={price} onChangeText={(value) => setPrice(currencyInput(value))} placeholder="0,00" keyboardType="numeric" /><Help>Digite os centavos sem pontuação: 7295 vira 72,95.</Help><Field label="Taxas" value={fees} onChangeText={(value) => setFees(currencyInput(value))} placeholder="0,00" keyboardType="numeric" optional /><Field label="Observação" value={note} onChangeText={setNote} placeholder="Opcional" optional autoCapitalize="sentences" /></View>{preview ? <Card className="mb-4" style={{ backgroundColor: "#DFF4FA", borderColor: "#A7DDE8" }}><Text className="text-xs font-medium uppercase tracking-wider text-muted">Prévia automática</Text><View className="mt-3 flex-row justify-between"><View><Text className="text-xs text-muted">Cotas após salvar</Text><Text className="mt-1 text-lg font-bold text-foreground">{number(preview.quantity)}</Text></View><View><Text className="text-xs text-muted">Preço médio</Text><Text className="mt-1 text-lg font-bold text-foreground">{currency(preview.averagePrice)}</Text></View><View><Text className="text-xs text-muted">Custo em aberto</Text><Text className="mt-1 text-lg font-bold text-foreground">{currency(preview.costBasis)}</Text></View></View></Card> : null}{error ? <Text className="mb-4 rounded-xl bg-error/10 p-3 text-sm text-error">{error}</Text> : null}<Button title={old ? "Salvar alterações" : "Salvar movimentação"} onPress={save} /><View className="h-4" /><Button title="Cancelar" variant="secondary" onPress={() => router.back()} /></ScrollView></ScreenContainer>; }
