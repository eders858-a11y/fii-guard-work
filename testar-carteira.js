const BRAPI_TOKEN = "fZh138TebUi2JYGBJG75C6"; const tickers = ["VGIR11", "RBRX11", "HGLG11", "HGBS11", "MFII11"]; async function run() { for(const t of tickers) { const r = await fetch(`https://brapi.dev/api/quote/${t}?dividends=true&token=${BRAPI_TOKEN}`); const j = await r.json(); const count = j?.results?.[0]?.dividendsData?.cashDividends?.length || 0; console.log(`Ticker ${t}: ${count} proventos encontrados`); } } run();
